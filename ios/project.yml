name: SunsetRipple
options:
  bundleIdPrefix: host.msknet
  deploymentTarget:
    iOS: "15.0"
  xcodeVersion: "15.0"
targets:
  SunsetRipple:
    type: application
    platform: iOS
    deploymentTarget: "15.0"
    sources:
      - SunsetRipple
    settings:
      base:
        PRODUCT_BUNDLE_IDENTIFIER: host.msknet.sunsetripple
        PRODUCT_NAME: SunsetRipple
        SWIFT_VERSION: "5.9"
        CODE_SIGNING_ALLOWED: NO
        CODE_SIGNING_REQUIRED: NO
        CODE_SIGN_IDENTITY: ""
        GENERATE_INFOPLIST_FILE: NO
        INFOPLIST_FILE: SunsetRipple/Info.plist
        DEVELOPMENT_TEAM: ""
        ENABLE_BITCODE: NO

