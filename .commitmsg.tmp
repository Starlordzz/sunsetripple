fix(ui): 修掉转场里的三个实际问题，并补上端到端与溢出回归测试

- 头部标题定位错了：`bottom: 0` 落在整屏 Stack 上，等于把房名/大标题钉在
  屏幕底部而不是头图底边。改成把标题与图标塞进一个与头图等高的 Stack，
  天空长高时它们自然跟着走，Transform 补偿也就不需要了。
- 离开房间的退场动画被 `await session.leave()` 挡在网络收尾之后，点了按钮
  要等一次 socket 关闭才动。改成动画立刻起、收尾在后台并行做。
- 窄屏溢出两处：底部控制条三个按钮在 360dp 上横向溢出 18px，改为均分宽度
  加 FittedBox；房型卡标题在长文案下会溢出，加 Flexible + 省略号。

新增测试：
- room_entry_transition_test：真的点「创建 WiFi 房」，逐帧走完整段转场，
  断言首页那组走干净、房间那组到齐，返回时原路退回；每帧都查溢出。
- room_layout_test：房内布局与首页整场在 360/411/430 三种宽度上都不溢出。
