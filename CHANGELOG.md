# Changelog

## 0.1.0-alpha.10 - 2026-09-02

### 新功能

- **统一直连与 WiFi 房（UI 与系统共用）**：
  - 移除孤立的「直连加速」开关，将 Wi-Fi Direct 直连完全收敛统一入标准的「WiFi 房」中。直连即为 WiFi 房的一种免路由网络拓扑。
  - 创建 WiFi 房时，后台同时拉起局域网组播广播与原生 Wi-Fi Direct 群组（Group Owner），无需用户手动切换链路。
  - 搜房列表统一将局域网广播房间与 Wi-Fi Direct 近场设备聚合展示为标准房间卡片，并标记「近场直连」。

### 修复与优化

- **修复离线 Wi-Fi Direct 房间发现与建组**：
  - 修复 `MainActivity.kt` 中 `WifiDirectPlugin` 遗漏向 Flutter 引擎注册导致原生 P2P 扫描与建组调用静默失效的根本缺陷。
  - 适配 Android 14 / ColorOS 14 系统广播安全隔离策略，将 P2P 广播接收器注册权限升级为 `RECEIVER_EXPORTED`。
  - 补齐并优化 `ACCESS_FINE_LOCATION` 与 `ACCESS_COARSE_LOCATION` 离线近场扫描权限申请。
  - 增强 `WifiDirectPlugin.kt` 底层遇到 `WifiP2pManager.BUSY` 状态时的退避重试机制与通道状态恢复。
  - 修复了未连 Wi-Fi 离线状态下 UDP 广播异常未捕获导致的控制台抛错。
- **实机与测试套件完善**：
  - 新增 Wi-Fi Direct 模块与平台通道单元测试，全套单测扩展至 87/87 全部通过。
  - 基于 UIAutomator2 对多台 Android 14 真机在完全无路由器、无热点的离线环境下完成端到端自动化建房、扫描与进房实测。

## 0.1.0-alpha.9 - 2026-08-30

### 新功能

- 进房动画换成一镜到底的「舞台」转场：首页与房间不再是两个页面，而是同一块画布上的两组前景。中间那层日轮/水波背景全程是同一个 `CelestialCanvas`，进房时只被挪位置、放大尺寸。首页的输入框、房型卡、按钮、房间列表依次下沉淡出，背景长高、天体上浮变大，房间的成员轨道、对讲盘、底部控制条再依次浮上来，三段刻意重叠，中间不留空档。编排表集中在 `lib/ui/transitions/stage_choreography.dart`。
- 昵称后面补回十六进制短码（`探索者#3F7A`）：房间里允许重名，成员轨道靠这串码区分同名的人。短码随昵称一起走 roster，协议未改；成员轨道上单独占一行、颜色淡一档，不会被昵称挤掉。
- 房内可读性整体上调：对讲盘 170→212、成员头像 48→64、底部控制条图标 18→24、房名 20→27、状态行 12→15，头图背景 260→348。以上尺寸均按屏幕高度取值并夹上下限，矮屏自动收窄。

### 修复与优化

- 修复进房转场掉帧：建房/加入时不再立刻开麦。`AudioRecord`/`AudioTrack` 的构造、AEC/NS/AGC 挂载与前台服务启动都压在 Android 主线程上，一次上百毫秒，塞进转场必然抢帧。改为动画落位后再调 `RoomSession.startAudio()`——该开关此前已存在，但首页一直没用上。
- 转场期间不再按帧重建整棵前景树：进出场包装件改为直接听 `Animation`，子树只建一次；两组前景的顶边固定，每帧重新布局的只剩头部那一个 `CustomPaint`。此前首页那棵树（含 `ListView` 与多个 `StreamBuilder`）每帧重建一次，是掉帧的另一个主因。
- 修复「离开房间」点了没反应：退场动画此前被 `await session.leave()` 挡在 socket 收尾之后，现在动画立刻起、音频与网络收尾在后台并行做。
- 修复房间头部标题定位错误：`bottom: 0` 落在整屏 Stack 上，把房名与大标题钉在了屏幕底部而不是头图底边。
- 修复窄屏溢出两处：360dp 上底部控制条三个按钮横向溢出 18px（改为均分宽度加 `FittedBox`）；房型卡标题在长文案下溢出（加 `Flexible` 与省略号）。
- 修复 `about_page.dart` 中 `withOpacity` → `withValues` 迁移残留的重复行，该文件此前无法通过 `flutter analyze`。
- 移除已无人引用的 `room_entry_reveal_route.dart`（旧的圆角矩形扩散式进房转场）。

### 变更

- `android/` 的 Gradle 仓库补上阿里云镜像，与根项目 `settings.gradle.kts` 保持一致：直连 `dl.google.com` 在国内会 TLS 握手中断，导致 `flutter build apk` 无法解析 AndroidX 构件。
- 新增测试：进房转场端到端验收（真的点「创建 WiFi 房」，逐帧走完整段转场并断言首尾状态与每帧不溢出）、房内与首页在 360/411/430 三种宽度下的溢出回归、编排表区间契约、短码拆分。测试总数 58 → 77。

## 0.1.0-alpha.8 - 2026-08-25

### 新功能

- 全量迁移至 Flutter：原 Kotlin 实现整体重写为 Flutter/Dart + C++ FFI 混合架构，Android、iOS 与 HarmonyOS NEXT 三端共用同一套 Dart 会话核心与二进制帧协议（`lib/core/`），仅音频 HAL 与 BLE 信道下沉到各平台原生层。
- 新增 C++ 无锁环形缓冲区（`native/src/ring_buffer.cpp`）与原生帧编解码（`native/src/protocol_frame.cpp`），经 `dart:ffi` 暴露给 Dart 层，压低音频路径抖动。
- 新增 BLE L2CAP CoC 传输通道（`lib/core/transport/ble_l2cap_transport.dart`），支撑蓝牙按住说话（PTT）房型。
- 新增统一平台音频通道（`lib/core/platform/platform_audio_channel.dart`），把三端原生硬件 AEC/NS/AGC 收敛到同一套接口。
- 引入 RoomCubit 会话状态管理，房主选举（`host_election.dart`）、房主转移（`host_transfer.dart`）与断线重连（`reconnect_controller.dart`）拆分为独立可测单元。
- WiFi 全双工房与蓝牙 PTT 房达成功能对齐，两种房型共享同一套成员轨道、全房静音联动与说话状态动画。
- 搜房界面补齐「重新扫描」入口，解决发现窗口超时后无法手动重试的问题。

### 修复与优化

- 修复平台音频通道在部分机型上通道名不匹配导致的采集异常。
- 界面文案去技术术语化，改用面向普通用户的表述。
- 新增 RoomSession 端到端集成测试与 PTT 状态测试，测试总数达到 58 个。

### 变更

- 修复 CI/CD Android 构建链：移除硬编码 `java.home`，改用 Flutter 官方 release action。
- 将 `intl` 依赖约束放宽为 `>=0.19.0 <1.0.0`，以兼容 Flutter SDK 内置 `flutter_localizations` 所钉的 intl 版本。
- CI 工作流统一钉定 Flutter 3.29.x：低于 3.27 会因缺少 `Color.withValues` 编译失败，而追随 latest stable 则会因其要求 Gradle >= 8.14 与 AGP 9 new DSL 而构建失败。
- GitHub Actions 依赖升级至当前最新主版本（checkout v7、setup-java v6、upload-artifact v7、download-artifact v8），消除 Node.js 20 运行时弃用告警。
- 全面重写 README 与 Wiki 文档，补充 iOS 安装包体积说明与 HarmonyOS 本地编译步骤。

## 0.1.0-alpha.7 - 2026-08-22

### 新功能

- 尝试多平台化（Core-Shell 统一多端架构）：引入跨平台协议与局域网发现，支持 Android、iOS 和 HarmonyOS NEXT。
- 新增跨端局域网/热点房间发现（`LanRoomDiscovery`），使不同品牌手机在同一 Wi-Fi 或热点下免网直连对讲。
- 新增 HarmonyOS NEXT 纯血鸿蒙原生工程（`harmonyos/`），基于 ArkTS/ArkUI 与 `@ohos.multimedia.audio` 硬件级 AEC。
- 新增 iOS 原生工程（`ios/`），基于 Swift/SwiftUI、`AudioUnit` 硬件 AEC 与 `MultipeerConnectivity` 免网 P2P。
- GitHub Actions CI/CD 流水线扩展为 Android、iOS、HarmonyOS NEXT 三条构建链。

### 修复与优化

- 优化夜间模式下「离开房间」按钮配色为冷调月夜玫瑰粉（`#FF7B92`），提升在深海蓝黑底色下的清晰度与可访问性对比度（> 7.5:1）。
- 修复客户端在按住 PTT 讲话时主机下行混音被静音的问题，支持讲话期间正常收听其他成员声音。

## 0.1.0-alpha.6 - 2026-08-20

### 修复

- 修复安卓与鸿蒙 4（NEXT 之前）手机在 WiFi 房互相搜不到的兼容性问题（房主建组后主动启动对等发现广播，解决厂商 P2P 协议栈需要组主处于扫描态方可响应 Probe Request 的限制）。
- 修复 WiFi 搜房界面缺少「重新扫描」按钮的问题（补充 `discovering` 状态监听与文案切换，解决系统发现窗口超时后页面定格的缺陷）。
- 修复房内「离开」按钮在深色/浅色模式下对比度过低、形似不可点击禁用态的问题（改用固定的浅珊瑚红配色）。
- 丰富 P2P 错误码转译语义（`ERROR` / `P2P_UNSUPPORTED` / `BUSY` / `NO_SERVICE_REQUESTS`）及对端设备发现与连接广播日志。

### 变更

- 整理并脱敏公开文档中的开发机路径，补齐 `.hprof` 忽略规则，将内部规划文档移出仓库跟踪。

## 0.1.0-alpha.5 - 2026-08-18

### 新功能

- 首页新增“关于与更新”入口，展示版本和 GitHub 项目，并可展开查看 CHANGELOG、许可证和隐私说明。
- 新增签名更新清单、APK 哈希/包名/证书校验、GitHub 下载和系统安装确认。
- 新增系统自动中英文、设备指纹、安全握手与 AES-GCM 密封帧核心。
- 新增网络质量指标、诊断导出、GitHub Issue 预填、Macrobenchmark 与 Baseline Profile。

### 变更

- 启动应用编排拆分：关于页更新状态和适配器失败事件由纯 Kotlin coordinator 统一管理，Compose 只收集不可变状态并转发事件。
- 导航状态统一交给纯 Kotlin coordinator；Activity 重建时恢复关于页，房间、扫描和回环等瞬态页面安全回到首页。
- 应用昵称、提示、主客意图和扬声器选择改由不可变 `AppUiState` 管理；主题状态、系统语言策略和权限等待动作均有独立纯 Kotlin 模块。
- 三类房间的会话所有权与释放顺序收口到 `RoomLifecycleCoordinator`，WiFi 房主转移保留组网和客户端连接身份守卫保持不变。
- 更新模块的 `check/download/install` 契约已由 GitHub adapter 实现；通话期间禁止检查和下载。
- 四类权限与蓝牙可发现性 launcher 移入独立 Android adapter；Activity/进程重建遇到房间、扫描或回环等瞬态页面时安全回首页并提示连接已结束。

### 已知限制

- 阶段 4 的三机以上真机矩阵按本轮范围跳过，连续交接和锁屏恢复仍未验收。
- 安全核心已有测试；三套传输的强制加密切换仍需兼容性验证后启用。
- Macrobenchmark 可编译，真实帧时间与端到端延迟必须在目标真机运行后记录。
- 发布构建必须通过 `sunsetRipple.updatePublicKey` 注入更新验签公钥。

## 0.1.0-alpha.4 - 2026-08-17

### 新功能

- 新增夜间配色「月与海面」：夜空靛蓝压到深海近黑，页头那轮天体成为冷月白的月亮，其下三道波纹变为月光在水面的反射。界面几何与版式完全不变，只更换配色。
- 新增昼夜三档切换，入口在首页页头右上角：跟随系统、始终浅色、始终深色，点击循环，图标本身即当前档位。选择写入本地存储，重装前一直保留。
- 自动档跟随系统深色开关，系统切换时界面实时更新，通话过程中切换不会中断连接。

### 变更

- 配色实现从全局常量改为调色板对象，浅色与夜间共用同一批槽位语义，两套主题复用同一份绘制代码。
- 浅色态顺手微调：次要文字对比度由 4.6:1 提升到 5.3:1，分隔线与发言指示色各加深一档，落日基调保持不变。
- 补充深色窗口主题，系统处于深色时冷启动不再短暂闪出浅色背景。

### 修复

- 修复扫描页提示条、麦克风检查按钮与房内工具栏选中态在深色配色下前景与底色同为深色、文字难以辨认的问题。

### 已知限制

- 夜间配色仅在模拟器上完成视觉核对，尚未在真机上验收。
- 异常接管依赖客户端已收到最新快照；房间刚建立即断电、所有候选设备同时离线或无线环境完全隔离时无法恢复。
- WiFi 房转移需要重建 WiFi Direct 组，期间语音会短暂中断，系统可能再次显示连接确认。
- Nearby 房已无限期搁置，当前版本不在 App UI 中显示入口。
- 锁屏通知展示和通知按钮交互已无限期搁置；屏幕关闭后的后台音频保活代码仍保留。
- WiFi 与蓝牙三机连续 A→B→C 转移、WiFi 系统确认弹窗和语音恢复耗时仍需真机验收，本版本仅作为 alpha 测试版本发布。

## 0.1.0-alpha.3 - 2026-08-16

### 变更

- 建房动画改为从实际点击位置直接展开完整房间界面，首页与房间共用连续的落日页头运动和同源配色，不再先播放独立遮罩再切页。
- 重新设计房内成员轨道、频道核心和底部操作栏；静音、扬声器与离开改为轻量图标控件，并降低危险操作的视觉体量。
- 统一应用横幅、启动画面、图标与房间界面的太阳和波纹几何，减少各入口之间的视觉割裂。

### 性能优化

- 将页头、状态波纹和圆形揭示的逐帧状态读取收敛到绘制层，避免动画驱动整棵 Compose 页面重组。
- 非活跃状态不再运行无效的无限波纹动画，并缓存圆形揭示路径与描边对象，减少转场期分配和绘制开销。
- 展开曲线改为即时响应后自然减速，缩短拖尾并降低建房动画的卡顿感。

### 修复

- 修复建房动画结束时短暂露出首页、闪黑或房间页再次弹出的割裂切换。
- 修复房内选中状态标签在深色底部栏上对比度不足的问题。

### 已知限制

- 异常接管依赖客户端已收到最新快照；房间刚建立即断电、所有候选设备同时离线或无线环境完全隔离时无法恢复。
- WiFi 房转移需要重建 WiFi Direct 组，期间语音会短暂中断，系统可能再次显示连接确认。
- Nearby 房已无限期搁置，当前版本不在 App UI 中显示入口。
- 锁屏通知展示和通知按钮交互已无限期搁置；屏幕关闭后的后台音频保活代码仍保留。
- WiFi 与蓝牙三机连续 A→B→C 转移、WiFi 系统确认弹窗和语音恢复耗时仍需真机验收，本版本仅作为 alpha 测试版本发布。

## 0.1.0-alpha.2 - 2026-08-15

### 新功能

- 应用处于前台时保持屏幕常亮，切换到后台后仍允许系统正常熄屏。

### 变更

- Android 安装标识更新为 `host.msknet.sunsetripple`；旧测试包 `com.wt.intercom` 无法覆盖升级，安装前需先卸载。

### 修复

- 修复客户端主动离房时，底层同步断线回调可能误触发房主接管或异常结束提示的问题。

### 已知限制

- 异常接管依赖客户端已收到最新快照；房间刚建立即断电、所有候选设备同时离线或无线环境完全隔离时无法恢复。
- WiFi 房转移需要重建 WiFi Direct 组，期间语音会短暂中断，系统可能再次显示连接确认。
- Nearby 房已无限期搁置，当前版本不在 App UI 中显示入口。
- 锁屏通知展示和通知按钮交互已无限期搁置；屏幕关闭后的后台音频保活代码仍保留。
- WiFi 与蓝牙三机连续 A→B→C 转移、WiFi 系统确认弹窗和语音恢复耗时仍需真机验收，本版本仅作为 alpha 测试版本发布。

## 0.1.0-alpha.1 - 2026-08-14

### 新功能

- 支持无需路由器的 WiFi Direct 全双工语音房，最多 6 台设备。
- 支持蓝牙 RFCOMM 按住说话房间，包含成员状态与有界音频队列。
- 支持普通成员短暂断线后的 1/2/4 秒重试、身份恢复与重连状态显示。
- WiFi 与蓝牙房主主动离房时，自动把房主转给仍在线且最早入房的成员；后续转移继续沿用原始入房顺序。
- 客户端缓存最新房主接管快照；房主进程崩溃、被系统终止或主链路突然断开且普通重连耗尽后，自动选出继任者并重建房间。
- 蓝牙房自动重建 RFCOMM 星型连接；WiFi 房自动重建 WiFi Direct 组并按继任者设备地址重连。
- 支持音频焦点、只听模式、前台通话服务、屏幕关闭后的音频保活与固定竖屏。
- 使用“落日后残波”品牌视觉，提供响应式圆形房间动作盘、触点扩散转场和极简对讲机房间界面。

### 修复

- WiFi Direct 组主地址长时间未就绪时，10 秒后给出明确错误并安全返回首页。
- 音频采集或播放持续失败时可靠结束会话，避免界面显示仍在通话。
- WiFi 与蓝牙成员重连时复用原身份，主动离房仍立即释放成员位置。
- 交接后的成员 ID 和入房顺序保持稳定，未重连成员的预留在 7 秒后释放，避免永久占用房间容量。

### 已知限制

- 异常接管依赖客户端已收到最新快照；房间刚建立即断电、所有候选设备同时离线或无线环境完全隔离时无法恢复。
- WiFi 房转移需要重建 WiFi Direct 组，期间语音会短暂中断，系统可能再次显示连接确认。
- Nearby 房已无限期搁置，当前版本不在 App UI 中显示入口。
- 锁屏通知展示和通知按钮交互已无限期搁置；屏幕关闭后的后台音频保活代码仍保留。
- WiFi 与蓝牙三机连续 A→B→C 转移、WiFi 系统确认弹窗和语音恢复耗时仍需真机验收，本版本仅作为 alpha 测试版本发布。
