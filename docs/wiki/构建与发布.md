# 构建与发布

本页覆盖从源码到签名 APK 的完整链路。所有命令均以仓库根目录为工作目录。

## 工具链版本

| 组件 | 版本 | 位置 |
| --- | --- | --- |
| JDK | 17 | 构建前需设置 `JAVA_HOME` |
| Gradle | 8.9（wrapper 自带） | `gradle/wrapper/gradle-wrapper.properties` |
| Android Gradle Plugin | 8.5.2 | `build.gradle.kts` |
| Kotlin | 2.0.20 | `build.gradle.kts` |
| Compose Compiler | Kotlin 2.0.20 内置插件 | `org.jetbrains.kotlin.plugin.compose` |

## 应用坐标

| 项 | 值 |
| --- | --- |
| `applicationId` / `namespace` | `host.msknet.sunsetripple` |
| `minSdk` | 26（Android 8.0） |
| `targetSdk` / `compileSdk` | 35 |
| `versionCode` / `versionName` | 5 / `0.1.0-alpha.4` |
| Java / JVM target | 17 |

> 旧测试包名 `com.wt.intercom` 与当前包名不同，**无法覆盖升级**，需先卸载旧版本。

## 依赖

| 依赖 | 版本 | 用途 |
| --- | --- | --- |
| `androidx.compose:compose-bom` | 2024.09.00 | Compose 版本对齐 |
| `androidx.activity:activity-compose` | 1.9.2 | Compose Activity 宿主 |
| `androidx.compose.material3:material3` | BOM 管理 | UI 组件 |
| `io.github.jaredmdobson:concentus` | 1.0.2 | 纯 JVM Opus 编解码，免 NDK |
| `org.jetbrains.kotlinx:kotlinx-coroutines-android` | 1.8.1 | 并发与流 |
| `androidx.core:core-ktx` | 1.13.1 | 平台扩展 |
| `androidx.lifecycle:lifecycle-runtime-compose` | 2.8.5 | 生命周期感知状态收集 |
| `com.google.android.gms:play-services-nearby` | 19.3.0 | Nearby 房（已搁置，代码保留） |
| `junit:junit` | 4.13.2 | 单元测试 |

选用 Concentus 而非原生 libopus，是为了让构建不依赖 NDK、产物不含 `.so`，代价是编解码走 JVM、CPU 开销略高。

## 依赖解析与国内镜像

`settings.gradle.kts` 把阿里云镜像放在 `google()` / `mavenCentral()` **之前**：

```kotlin
maven { url = uri("https://maven.aliyun.com/repository/google") }
maven { url = uri("https://maven.aliyun.com/repository/central") }
google()
mavenCentral()
```

原因：部分网络环境直连 `dl.google.com` 会 TLS 中断。镜像优先可命中缓存，官方仓库保留在后作为后备，镜像未同步的构件仍能回落直连。

`dependencyResolutionManagement` 设为 `FAIL_ON_PROJECT_REPOS`，模块级 `repositories {}` 会直接构建失败，仓库配置只有 `settings.gradle.kts` 一个来源。

## 日常验证

Windows PowerShell：

```powershell
$env:JAVA_HOME='D:\LEARNING\tools\jdk-17'
.\gradlew.bat :app:testDebugUnitTest :app:assembleDebug :app:lintDebug
```

macOS / Linux：

```bash
export JAVA_HOME=/path/to/jdk-17
./gradlew :app:testDebugUnitTest :app:assembleDebug :app:lintDebug
```

三个任务分别覆盖：单元测试、Debug 打包、Lint 静态检查。提交前应三者全绿。

## 发布签名

### 密钥与配置

签名配置和密钥**只存在于本地**，不进入版本库。`.gitignore` 已排除 `keystore.properties`、`*.jks`、`*.keystore`、`*.p12`。

`keystore.properties` 格式见 `keystore.properties.example`：

```properties
storeFile=release/sunset-ripple-release.p12
storePassword=<强密码>
keyAlias=sunset-ripple
keyPassword=<强密码>
```

> **同一密钥必须长期沿用。** Android 以签名证书标识应用来源，换密钥后已安装的用户无法覆盖升级，只能卸载重装。请离线备份 `.p12` 与密码。

### 签名守卫

`app/build.gradle.kts` 注册了 `verifyReleaseSigning` 任务，并挂在 `packageRelease` / `bundleRelease` 之前。缺少配置文件、缺字段或密钥文件不存在时，构建会**提前失败**并给出中文原因，避免产出未签名或错签的包。

### 打包

```powershell
$env:JAVA_HOME='D:\LEARNING\tools\jdk-17'
.\gradlew.bat :app:testDebugUnitTest :app:lintRelease :app:assembleRelease :app:bundleRelease
```

产物：

- 可侧载 APK：`app/build/outputs/apk/release/app-release.apk`
- 应用商店 AAB：`app/build/outputs/bundle/release/app-release.aab`

CI 或临时签名配置可用 Gradle 属性指向别处（接受绝对路径）：

```bash
./gradlew :app:assembleRelease -PsunsetRipple.signingProperties=/secure/path/keystore.properties
```

### 发布到 GitHub Release

已发布版本见 [Releases](https://github.com/Starlordzz/sunsetripple/releases)。惯例：

1. 更新 `app/build.gradle.kts` 的 `versionCode` / `versionName`。
2. 在 `CHANGELOG.md` 顶部新增版本段落（新功能 / 变更 / 修复 / 已知限制）。
3. 提交 `chore: release vX.Y.Z`，打同名标签 `vX.Y.Z`。
4. 上传签名 APK 与 AAB，并为附件添加包含版本号的展示标签。

```bash
awk '/^## 0.1.0-alpha.4/{found=1} found && /^## / && !/^## 0.1.0-alpha.4/{exit} found' \
  CHANGELOG.md > /tmp/sunset-ripple-alpha.4-notes.md

gh release create v0.1.0-alpha.4 \
  'app/build/outputs/apk/release/app-release.apk#SunsetRipple-v0.1.0-alpha.4.apk' \
  'app/build/outputs/bundle/release/app-release.aab#SunsetRipple-v0.1.0-alpha.4.aab' \
  --title v0.1.0-alpha.4 --notes-file /tmp/sunset-ripple-alpha.4-notes.md \
  --prerelease
```

alpha / beta 版本记得带 `--prerelease`，以免被标成 Latest。

## Alpha 5 开发分支

`alpha5-about-update` 当前只接入关于页和可替换的更新状态模型。更新协议仍需完成清单签名、SHA-256 校验、下载和系统安装确认后，才能进入正式发布门禁。

## 相关页面

- [常见问题](常见问题.md)——构建报错与安装失败的快速排查
- [故障排查](故障排查.md)——运行期问题
- [架构总览](架构总览.md)
